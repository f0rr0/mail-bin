export function App() {
	return (
		<main className="min-h-screen bg-[var(--app-bg)] text-[var(--app-ink)]">
			<div className="mx-auto flex min-h-screen max-w-5xl items-center justify-center px-6">
				<div className="space-y-4 text-center">
					<p className="text-xs uppercase tracking-[0.35em] text-[var(--app-muted)]">
						Mail Bin
					</p>
					<h1 className="text-4xl font-semibold tracking-tight">
						Project skeleton is loading.
					</h1>
					<p className="mx-auto max-w-xl text-sm text-[var(--app-muted)]">
						The worker, database, UI, and tooling are being wired into the
						disposable mail router now.
					</p>
				</div>
			</div>
		</main>
	);
}
